<?php

/**
 * Implements hook_init().
 */
function max_file_size_changer_init() {
  // Set the max upload file size (e.g., 50MB).
  ini_set('upload_max_filesize', '500M');

  // Set the max POST size (must be larger than the max file size).
  ini_set('post_max_size', '500M');
}

